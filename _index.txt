<template>
  <view class="entry">
    <view class="bg"></view>
    <view class="center">
      <text class="emoji">🧰</text>
      <text class="title">工具箱</text>
      <text class="desc">正在加载...</text>
    </view>
  </view>
</template>
<script setup>
import { onMounted } from 'vue';
onMounted(() => {
  const isLoggedIn = uni.getStorageSync('isLoggedIn');
  const isTab = getCurrentPages().length > 0 && getCurrentPages()[0].route === 'pages/index/index';
  setTimeout(() => {
    if (isLoggedIn) {
      uni.switchTab({ url: '/pages/home/home' });
    } else {
      uni.reLaunch({ url: '/pages/login/login' });
    }
  }, 800);
});
</script>
<style lang="scss" scoped>
.entry{
  width:100vw;height:100vh;display:flex;align-items:center;justify-content:center;position:relative;
}
.bg{position:absolute;inset:0;background:linear-gradient(135deg,#4a6cf7 0%,#7c3aed 100%);z-index:0;}
.center{position:relative;z-index:1;display:flex;flex-direction:column;align-items:center;}
.emoji{font-size:100rpx;margin-bottom:24rpx;}
.title{font-size:48rpx;color:#fff;font-weight:700;letter-spacing:2rpx;}
.desc{font-size:26rpx;color:rgba(255,255,255,.7);margin-top:16rpx;}
</style>
